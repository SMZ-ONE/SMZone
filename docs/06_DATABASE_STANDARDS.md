# 06 - DATABASE STANDARDS
- Table names plural, Model singular.
- Foreign keys: `user_id`, later `team_id` (nullable until Phase 4).
- Index all foreign keys and status columns.
- Use `json` column for flexible brand settings: `brand_settings`
